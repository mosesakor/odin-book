<% @current_user.friend_requests.each do |user| %>
  <p class="title is-4"> <%= user.first_name %> </p>
  <p class="subtitle is-6"> <%= user.email %> </p>
  <%= link_to "Accept Request", accept_friend_request_path(user.id, current_user.id), method: :put %>
<% end %>
